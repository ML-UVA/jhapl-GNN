"""
null_analysis: Motif census, network metrics, and null-model comparison for the jhapl-GNN connectome.

Modules:
- config: Global analysis constants (N_BINS, N_NULLS, RANDOM_SEED).

Subpackages:
- binning: Spatial feature binning and empirical P(edge | feature) computation.
- metrics: Network analysis metrics (motif counts, clustering, degree statistics).
- null_models: Random graph generators and the unified null-model interface.
"""
