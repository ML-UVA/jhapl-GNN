from setuptools import setup, find_packages

setup(
    name="spatial_training",
    version="1.0.0",
    description="JHU APL Connectomics GNN Pipeline",
    packages=find_packages(), # This automatically finds folders with __init__.py
)